# PX4 Development Environment Configuration
# Copy this file to .env and customize for your setup
#
# Usage: cp env.example .env

# ==============================================
# Vehicle Configuration
# ==============================================

# Unique vehicle identifier (1-255)
VEHICLE_ID=1

# Airframe model
# Options: x500, iris, typhoon_h480, standard_vtol, plane
AIRFRAME=x500

# MAVLink system ID (should match VEHICLE_ID for single vehicle)
MAV_SYS_ID=1

# ==============================================
# Simulation Mode
# ==============================================

# Simulation mode: sitl, hitl, or dev
SIM_MODE=sitl

# ==============================================
# Network Ports
# ==============================================

# QGroundControl MAVLink port
QGC_PORT=14550

# MAVSDK / Offboard API port
MAVSDK_PORT=14540

# SITL communication port
SITL_PORT=14580

# ==============================================
# Serial Configuration (HITL Mode)
# ==============================================

# Serial device for Cube+ Orange
# Use /dev/cube_orange if udev rules are installed
SERIAL_DEVICE=/dev/cube_orange

# Serial baud rate
SERIAL_BAUD=921600

# ==============================================
# Display Configuration
# ==============================================

# X11 display for Gazebo GUI
# Usually :0 for local display, :1 for remote
DISPLAY=:0

# X authority file path (usually auto-detected)
# XAUTHORITY=/home/username/.Xauthority

# ==============================================
# GPU / Rendering Configuration
# ==============================================

# Force software rendering (set to 1 if GPU issues occur)
LIBGL_ALWAYS_SOFTWARE=0

# ==============================================
# Raspberry Pi Connection (External Clients)
# ==============================================
# These settings are used when connecting from a Raspberry Pi
# or other external device via TCP, WiFi (UDP), or UART.
# See docs/PI_SETUP.md for complete setup instructions.

# Connection type: tcp (recommended), wifi, or uart
# PI_CONNECTION_TYPE=tcp

# TCP settings (recommended for remote connections)
# PI_TCP_HOST=192.168.1.100
# PI_TCP_PORT=5760

# WiFi (UDP) settings (for same-network, low latency)
# PI_WIFI_HOST=192.168.1.100
# PI_WIFI_PORT=14540

# UART settings (for production with Cube+ Orange TELEM2)
# PI_UART_DEVICE=/dev/ttyAMA0
# PI_UART_BAUD=57600

# Host ports exposed for external MAVLink clients
# These are mapped from the px4-sitl container to the host
PI_TCP_PORT=5760
PI_MAVLINK_PORT=14540
PI_QGC_PORT=14551

# ==============================================
# Advanced Settings
# ==============================================

# PX4 firmware target for Cube+ Orange
# PX4_FMU_TARGET=px4_fmu-v6x_default

# Enable verbose logging
# PX4_VERBOSE=1

